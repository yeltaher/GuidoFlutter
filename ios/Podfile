# Set minimum platform version for Guido Companion App (Flutter + Unity UaaL)
platform :ios, '15.0'

# CocoaPods analytics sends network stats synchronously affecting flutter build latency.
ENV['COCOAPODS_DISABLE_STATS'] = 'true'

project 'Runner', {
  'Debug' => :debug,
  'Profile' => :release,
  'Release' => :release,
}

def flutter_root
  generated_xcode_build_settings_path = File.expand_path(File.join('..', 'Flutter', 'Generated.xcconfig'), __FILE__)
  unless File.exist?(generated_xcode_build_settings_path)
    raise "#{generated_xcode_build_settings_path} must exist. If you're running pod install manually, make sure flutter pub get is executed first"
  end

  File.foreach(generated_xcode_build_settings_path) do |line|
    matches = line.match(/FLUTTER_ROOT\=(.*)/)
    return matches[1].strip if matches
  end
  raise "FLUTTER_ROOT not found in #{generated_xcode_build_settings_path}. Try deleting Generated.xcconfig, then run flutter pub get"
end

require File.expand_path(File.join('packages', 'flutter_tools', 'bin', 'podhelper'), flutter_root)

flutter_ios_podfile_setup

target 'Runner' do
  use_frameworks!
  use_modular_headers!

  flutter_install_all_ios_pods File.dirname(File.realpath(__FILE__))

  target 'RunnerTests' do
    inherit! :search_paths
  end
end

pre_install do |installer|
  # Security check: verify if UnityLibrary/Data assets are Git LFS pointers (e.g. size < 500 bytes)
  unity_data_dir = File.expand_path(File.join(File.dirname(__FILE__), 'UnityLibrary', 'Data'))
  if Dir.exist?(unity_data_dir)
    sample_asset = File.join(unity_data_dir, 'sharedassets0.assets')
    lfs_pointer_detected = (File.exist?(sample_asset) && File.size(sample_asset) < 500) ||
      Dir.glob(File.join(unity_data_dir, '*.assets')).any? { |f| File.file?(f) && File.size(f) < 500 }

    if lfs_pointer_detected
      puts '[Podfile] Detected Git LFS pointer for UnityLibrary/Data. Pulling actual binaries with git lfs pull...'
      system('git lfs pull')
    end
  end
end

post_install do |installer|
  installer.pods_project.targets.each do |target|
    flutter_additional_ios_build_settings(target)

    target.build_configurations.each do |config|
      # Force iOS 15.0 deployment target across all pods
      config.build_settings['IPHONEOS_DEPLOYMENT_TARGET'] = '15.0'

      # Disable Bitcode (deprecated in Xcode 15+, unsupported by modern UnityFramework)
      config.build_settings['ENABLE_BITCODE'] = 'NO'

      # Support UnityFramework search paths for flutter_unity_widget
      config.build_settings['FRAMEWORK_SEARCH_PATHS'] ||= ['$(inherited)']
      config.build_settings['FRAMEWORK_SEARCH_PATHS'] << '"${PODS_ROOT}/../UnityLibrary"'
      config.build_settings['FRAMEWORK_SEARCH_PATHS'] << '"${PODS_CONFIGURATION_BUILD_DIR}"'
    end
  end

  # Force iOS 15.0 deployment target at the Pods project level
  installer.pods_project.build_configurations.each do |config|
    config.build_settings['IPHONEOS_DEPLOYMENT_TARGET'] = '15.0'
  end

  # Ensure all aggregate targets (such as Pods-Runner) also enforce iOS 15.0
  if installer.respond_to?(:aggregate_targets)
    installer.aggregate_targets.each do |target|
      if target.respond_to?(:xcconfigs)
        target.xcconfigs.each do |config_name, config_file|
          config_file.attributes['IPHONEOS_DEPLOYMENT_TARGET'] = '15.0'
        end
      end
    end
  end
end